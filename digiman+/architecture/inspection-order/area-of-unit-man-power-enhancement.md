# Enhancement: Area of Unit & Man Power — Inspection & Order

*Last updated: 2026-07-17*

---

**Feature:** Inspection & Order (Digiman+)
**Related doc:** [../dplan/digital-planning.md](../dplan/digital-planning.md), [../dplan/man-power-man-hours-excel-enhancement.md](../dplan/man-power-man-hours-excel-enhancement.md), [maintenance-activity-type-enhancement.md](maintenance-activity-type-enhancement.md) *(enhancement terpisah, menyentuh layar sama persis — Additional Order screen 1 & 2, create eMOL Inspection — lihat catatan koordinasi di 2.3)*
**Effort summary lintas fitur:** [../area-of-unit-man-power-effort-summary.md](../area-of-unit-man-power-effort-summary.md) *(belum mencakup effort [maintenance-activity-type-enhancement.md](maintenance-activity-type-enhancement.md))*

---

## 1. Latar Belakang

Hasil meeting dengan business (10 Jul 2026) terkait penambahan konsep **Area of Unit** — pemetaan lokasi kerusakan/pekerjaan ke tingkat component/sub-component suatu asset — dan **Man Power**, serta enhancement terkait di beberapa fitur: Inspection, Order, Approval Order, dan Digiplan. *(Klarifikasi lanjutan 14 Jul 2026: Area of Unit dipahami sebagai grouping lokasi yang lebih besar/coarse-grained dibanding Component/Sub-Component — lihat detail relasi & dampaknya ke cascading dropdown di 2.2.)*

---

## 2. Keputusan & Scope

### 2.1 Penamaan Field
- Nama field/kolom yang disepakati: **"Area"** of unit (bukan "Compartment" — istilah lama yang digantikan).

### 2.2 Master Data Mapping Area of Unit ↔ Component/Sub-Component
- **Master data Component↔Sub-Component sudah ada** — yang perlu dibuat baru hanyalah **mapping Area ke baris `ModelComponentSubComponent`** (kombinasi Model+Component+SubComponent), bukan ke pasangan Component-SubComponent lepas (bukan membangun 3 level master data dari nol).
- **Component-SubComponent existing itu ter-mapping ke Model of Equipment/Asset Model** *(dikonfirmasi 10 Jul 2026)* — bukan master list flat global. Artinya kombinasi Component-SubComponent yang valid **berbeda-beda tergantung Asset Model** unit yang diinspeksi/di-plan.
- **Relasi many-to-many — status: superseded (16 Jul 2026)** *(sempat dikonfirmasi 10 Jul 2026)*: sebelumnya disimpulkan 1 kombinasi Component-SubComponent bisa terpetakan ke beberapa Area dan sebaliknya (M:N). Setelah scoping per-model diputuskan (lihat poin "Relasi final" di bawah), kesimpulan M:N murni ini **tidak lagi berlaku** — relasinya ternyata 1:N begitu Model ikut jadi bagian dari kombinasi.
- **Area sebagai grouping yang lebih besar/coarse-grained** *(dikonfirmasi 14 Jul 2026)* — secara konsep bisnis **Area itu level yang lebih besar dibanding Component/Sub Component**: 1 Area lazimnya mencakup/menaungi banyak kombinasi Component-SubComponent (mis. Area "Engine Compartment" mencakup banyak component di dalamnya), bukan sebaliknya. Konsekuensinya, user secara alami berpikir "di Area mana" dulu sebelum masuk ke Component/SubComponent yang spesifik — ini yang mengubah **arah cascading dropdown** (lihat poin cascading di bawah, supersede urutan 10 Jul 2026). *(Ini soal urutan pengisian di UI — tidak berubah walau relasi datanya sekarang 1:N, lihat poin berikut.)*
- **✅ Relasi final: 1:N per baris `ModelComponentSubComponent`, bukan M:N (diputuskan 16 Jul 2026)** — karena Component/SubComponent **sudah** di-map ke Model (lewat `AssetModelMappingCode`, 5 dimensi: Type/Brand/Model/Variant/Class, lihat [`services-asset-schema.md`](../database/services-asset-schema.md)), 1 kombinasi **Model+Component+SubComponent** yang spesifik **tidak mungkin** berada di lebih dari 1 Area fisik sekaligus — 1 part fisik ada di 1 lokasi. Justifikasi yang dipakai: "Hydraulic Pump"+"Filter" bisa ada di Area "Engine Bay" di truck model A, tapi Area "Rear Compartment" di model B — tapi begitu model-nya dipin ke satu combo spesifik, Area-nya cuma 1, tidak ganda. Kesimpulan M:N "Area ↔ kombinasi Component-SubComponent" (10 Jul 2026) cuma muncul kalau Model diabaikan; begitu Model ikut serta sebagai bagian dari kombinasi, relasinya jadi 1:N.
  - **⚠️ Perlu diteliti lebih lanjut sebelum Master Data UI final** *(dicatat 16 Jul 2026, permintaan eksplisit saat diskusi)* — asumsi "1 kombinasi Model+Component+SubComponent = maksimal 1 Area" masuk akal secara logika tapi **belum divalidasi ke data real/mechanical SME**. Kalau ternyata ada edge case (mis. Component besar yang secara fisik membentang lintas 2 Area), asumsi 1:N ini perlu direvisi kembali ke M:N/junction table. Lihat juga open item Bagian 4.
  - **Desain skema**: **1 kolom baru `AreaCode`** (FK ke tabel master `Area` baru: Code, Name, standar) ditambahkan **langsung ke tabel `ModelComponentSubComponent`** — bukan tabel junction terpisah. Nullable, backward compatible untuk baris existing.
  - **Impact ke tabel existing**: `ModelComponentSubComponent` adalah tabel foundational yang sudah dipakai fitur lain (`RatingCategoryCode` untuk Rating saat inspeksi, `SubComponentDamage`/`DamageCode` lewat SubComponentCode, [`query_mapping_damage_code.sql`](../../query/query_mapping_damage_code.sql), proses onboarding model di [`new-model-checklist.md`](../database/new-model-checklist.md)). Menambah kolom nullable secara teknis **aman untuk data existing** (default NULL, tidak breaking query lama yang tidak eksplisit select kolom baru), tapi **blast radius regression testing lebih lebar** dibanding tabel baru terisolasi — semua fitur yang bergantung pada tabel ini idealnya ikut di-regression-check saat migrasi jalan, bukan cuma fitur Area.
  - **Konsekuensi maintenance data**: admin HO tetap harus define Area **per baris `ModelComponentSubComponent`** (per model) — kalau 10 model berbeda sama-sama punya Component+SubComponent yang sama, Area untuk tiap model itu perlu diisi terpisah (tidak otomatis ke-reuse lintas model). Menambah effort input data awal (rollout) dan menambah 1 step di [`new-model-checklist.md`](../database/new-model-checklist.md) setiap onboarding model baru — **✅ sudah ditambahkan ke checklist itu (16 Jul 2026)**.
  - **Opsi hybrid (default generik + override per model) sudah dipertimbangkan dan ditolak** *(diputuskan 16 Jul 2026)* — dianggap **tidak worth it**, kompleksitas lookup fallback (cari override dulu, baru fallback default) tidak sepadan dengan penghematan effort.
  - **Konsekuensi cascading dropdown — lebih simpel dari draf sebelumnya, dan sekarang cuma relevan untuk Digiplan** *(lihat poin berikut soal cakupan layar)*: karena Area sekarang kolom langsung di baris yang sama (bukan lewat tabel junction terpisah), resolusi Area→Component→SubComponent tidak butuh join tambahan. Untuk urutan "Area duluan" di Digiplan: `SELECT DISTINCT AreaCode` dari `ModelComponentSubComponent` yang difilter `AssetModelMappingCode` sesuai Model unit — 1 query filter langsung di tabel yang sama, bukan multi-hop lewat tabel lain. Di Inspection/Additional Order/Approval, arah query-nya malah kebalik: Component+SubComponent (yang dipilih user) → lookup `AreaCode` langsung dari baris yang cocok — bukan dipakai untuk narrow-down pilihan, cuma buat ambil nilai.
- Perlu dibuat **Master Data UI baru** untuk assign Area ke baris `ModelComponentSubComponent` yang sudah ada.
- **Service: `services-asset`** *(dikonfirmasi 11 Jul 2026)* — Master Data ini akan dikembangkan di service **Asset**, konsisten dengan lokasi master data Component-SubComponent existing yang juga ada di service yang sama (lihat data source `mkp_services_asset` yang dipakai di `vw_report_iams_inspection_results.sql`, tabel `assetmodel`, `component`, `subcomponent`, dst).
- **Yang maintain mapping ini** *(dikonfirmasi 10 Jul 2026)*: **admin di HO (Head Office)** — siapapun dengan role admin HO bisa maintain, dibatasi lewat **permission code** (bukan role/user spesifik yang di-hardcode).
- **Penyimpanan data transaksi: by value, bukan by ID reference** *(dikonfirmasi 10 Jul 2026)* — Component/Sub Component/Area yang tersimpan di data transaksi (Finding, eMOL, `PoolingMOItem`, `DPValue` di Digiplan, dst) disimpan sebagai **nilai/snapshot pada saat transaksi terjadi** (mis. nama Component sebagai text), **bukan** sebagai foreign key/ID yang mereferensikan baris Master Data. Konsekuensinya: kalau mapping Master Data berubah/dihapus di kemudian hari, **data transaksi historis tidak terpengaruh sama sekali** — tetap merepresentasikan kondisi valid saat transaksi itu terjadi, tidak jadi stale/orphan. Ini prinsip yang perlu diikuti di semua titik penyimpanan field ini (Inspection, Additional Order, `PoolingMOItem`, Digiplan).
- Enhance transaksi terkait untuk menggunakan mapping ini di: **Inspection**, **Order**, **Approval Order**.
- **✅ Area BUKAN dropdown yang dipilih user di Inspection/Additional Order/Order Approval (diputuskan 16 Jul 2026)** — ini koreksi besar dari keputusan 14 Jul 2026 sebelumnya (yang mengasumsikan Area jadi dropdown aktif di semua layar). Karena Area sekarang **1:N deterministik** dari Model+Component+SubComponent (lihat poin "Relasi final" di atas), tidak ada gunanya user pilih Area secara manual di layar-layar itu — begitu Component+SubComponent dipilih (cascading **2 level seperti pola existing sekarang**, TANPA Area), sistem tinggal **lookup `AreaCode`** dari baris `ModelComponentSubComponent` yang cocok dan simpan sebagai snapshot value di data transaksi. Area **tidak muncul sama sekali** di form input Inspection/Additional Order/Order Approval — murni logic backend, baru terlihat nanti di Digiplan/report.
- **Digiplan adalah SATU-SATUNYA layar yang tetap pakai Area sebagai dropdown aktif** *(dikonfirmasi 16 Jul 2026)* — urutan cascading di Digiplan tetap **Area → Component → Sub Component** (Area duluan, mengikuti klarifikasi bahwa Area adalah grouping yang lebih besar/coarse-grained, lihat poin di atas), difilter juga oleh Asset Model unit (**Asset Model → Area → Component → Sub Component**). Alasan Digiplan berbeda dari 3 layar lain: planner di Digiplan kerja di level lebih coarse/summary (assign & filter task lintas banyak component sekaligus), jadi Area berguna sebagai filter awal untuk mempersempit pilihan Component/SubComponent yang banyak. Sebaliknya di Inspection/Additional Order/Approval, user sudah bekerja di konteks Component+SubComponent yang presisi (tahu persis bagian mana yang di-inspeksi/di-order), jadi Area di situ cuma redundant noise. Detail lengkap di 2.4.
- **Auto-fill Area dari MO Backlog tidak bisa didapat dari Component/SubComponent saja** *(dikonfirmasi 10 Jul 2026, alasan diperbarui 16 Jul 2026)* — awalnya beralasan M:N, tapi setelah relasi final jadi 1:N per baris `ModelComponentSubComponent` (lihat 2.2), alasannya bergeser: Component/SubComponent saja **tidak membawa informasi Model**, padahal Area bergantung pada kombinasi Model+Component+SubComponent. Tanpa tahu Model-nya, sistem tidak bisa resolve baris `ModelComponentSubComponent` yang tepat untuk ambil `AreaCode`-nya. `Area` **wajib dikirim eksplisit** sebagai field tersendiri lewat pipeline Order→SAP→MO Backlog (bukan di-derive ulang dari Component/SubComponent saat data kembali) — memperkuat kenapa Area harus benar-benar jadi field sendiri di assessment SAP (2.5), bukan sekadar "ikut" Component/SubComponent.

### 2.3 Inspection, Additional Inspection & Additional Order
- Saat create finding (Inspection), saat ini **sudah ada** field **Component**, **Sub Component**, dan **Duration**.
- **Tambah field Man Power** di create finding — melengkapi 3 field yang sudah ada (Component, Sub Component, Duration tidak berubah, tetap cascading 2 level seperti sekarang). **✅ Area TIDAK ditambahkan sebagai field/dropdown di layar ini** *(dikonfirmasi 16 Jul 2026, koreksi dari asumsi sebelumnya)* — Area cukup di-derive otomatis di backend dari Component+SubComponent yang dipilih (lihat 2.2) dan disimpan sebagai snapshot value, tidak tampil/dipilih di form.
- **Additional Inspection memakai layar yang sama persis dengan Inspection** *(dikonfirmasi 11 Jul 2026)* — satu-satunya perbedaan adalah Additional Inspection bisa dibuat **kapan saja** (ad-hoc), sementara Inspection biasa hanya lewat jadwal (scheduled). Karena form create-finding-nya identik, **tidak perlu effort/baris terpisah** untuk Additional Inspection — penambahan Area + Man Power di form Inspection (baris di atas) otomatis berlaku untuk Additional Inspection juga.
- **Additional Order tetap perlu field yang sama secara terpisah** *(dikonfirmasi 10 Jul 2026)* — beda dengan Additional Inspection, Additional Order adalah **layar/form yang berbeda** dari Inspection (jalur kedua pembuatan eMOL tanpa finding, lihat [order-emol-sap-sync.md](order-emol-sap-sync.md) Bagian 2), jadi field **Component, Sub Component, Duration, Man Power** perlu ditambahkan terpisah di layar **create Additional Order** (cascading 2 level Component→SubComponent, sama seperti Inspection — **bukan** 4 field termasuk Area; Area di-derive backend saja, lihat 2.2). Kalau tidak, MO Backlog yang asalnya dari Additional Order akan selalu kosong untuk field-field tersebut — merusak tujuan utama enhancement ini (auto-fill di Digiplan).
- **Nilai ini otomatis terbawa ke edit eMOL** *(dikonfirmasi 10 Jul 2026)* — pola-nya **carry-forward-with-override**, bukan re-input terpisah: nilai yang diisi saat create Finding/Additional Order **otomatis muncul** di layar edit eMOL (bukan kosong/harus diisi ulang), dan tetap bisa diubah di titik itu kalau perlu. Pola yang sama berlaku sampai ke Order Approval (2.6) — satu nilai mengalir maju dengan kesempatan override di tiap checkpoint, bukan beberapa copy independen yang perlu disinkronkan manual.
- **⚠️ Koordinasi dengan enhancement lain**: layar create Additional Order (screen 1 Asset Details & screen 2 per-eMOL block) yang dibahas di sini **juga jadi target perubahan** enhancement [maintenance-activity-type-enhancement.md](maintenance-activity-type-enhancement.md) — field "Activity Type" di screen 1 dihapus, block "Material Order #N" di screen 2 di-rename jadi "Order #N", dan field baru "Maintenance Activity Type" disisipkan di bawah Order Type. Kedua enhancement sebaiknya dikerjakan dalam sprint yang sama supaya layar yang sama tidak diubah dua kali secara terpisah.

### 2.4 Digiplan
- Tambah kolom **Component**, **Sub Component**, **Area** — mengikuti pola yang sama seperti di Inspection, sebagai dynamic column baru lewat mekanisme `DPColumn`/`DPValue` (lihat [digital-planning.md](../dplan/digital-planning.md)).
- **Level/scope**: **per-task (leaf level)** — sama seperti Duration/Man Power, setiap task punya nilai Component/Sub Component/Area sendiri (bukan diisi sekali di header Plan). Relevan karena satu Plan bisa mencakup pekerjaan di banyak component/area berbeda.
- **Cara pengisian**: **dropdown**, diambil dari Master Data mapping Area of Unit → Component/Sub-Component (lihat 2.2) — bukan free text manual. Urutan cascading dropdown **Area → Component → Sub Component** *(direvisi 14 Jul 2026)*. **✅ Digiplan adalah satu-satunya layar yang expose Area sebagai dropdown aktif** *(dikonfirmasi 16 Jul 2026)* — berbeda dari Inspection/Additional Order/Order Approval yang sekarang cukup cascading 2 level Component→SubComponent tanpa Area (lihat 2.2 & 2.3). Berlaku **konsisten untuk kedua kasus**: (1) task yang berasal dari MO Backlog — nilai auto-fill dari data MO Backlog (lihat 2.5); (2) task/subtask yang **tidak** berasal dari MO Backlog — user input manual **lewat dropdown Master Data yang sama** (Area duluan, baru Component/SubComponent), bukan free text, supaya datanya tetap konsisten/tervalidasi terlepas dari sumbernya. *(Dikonfirmasi ulang 10 Jul 2026 — sempat diangkat kembali sebagai open question, jawabannya tetap dropdown dari Master Data.)* Ini berarti pengembangan kolom ini **bergantung** pada Master Data UI (2.2) selesai/tersedia lebih dulu.
- **Mandatory/disable**: **custom column biasa** — bisa di-toggle (*Is Shown*) dan dihapus oleh admin lewat Config → Custom Column, **beda perlakuan dari Man Power/Man Hours** yang mandatory & non-disable (lihat [man-power-man-hours-excel-enhancement.md](../dplan/man-power-man-hours-excel-enhancement.md) 3.1). *(Dikonfirmasi 10 Jul 2026 — bukan kontradiksi dengan kegunaan kolom ini menerima data MO Backlog: berbeda dengan Man Power/Man Hours yang mandatory karena ada dependency kalkulasi (`Man Hours = Duration × Man Power`), Component/Sub Component/Area murni informasional tanpa dependency kalkulasi, jadi tidak butuh jaminan "selalu ada". Karena kolom Daily Plan pada dasarnya dinamis, logic yang men-treatment MO Backlog juga harus dinamis — lihat catatan graceful-handling di bawah.)*
- **Graceful handling saat kolom tidak ada di Plan**: kalau Template yang dipakai suatu Plan **tidak** punya kolom Component/Sub Component/Area (di-hide/dihapus admin), sementara MO Backlog yang dipilih user membawa data untuk field-field itu — sistem **harus bisa menghandle situasi ini**, bukan mewajibkan kolom harus selalu ada. Behavior-nya: field yang tidak ada `DPColumn`-nya di Plan tersebut **cukup diabaikan/tidak disimpan** (bukan error, bukan auto-create kolom baru di Plan) — sisanya (field yang memang ada kolomnya) tetap ter-fill normal.
- *(Data ini bisa langsung terisi otomatis saat user pilih MO Backlog di Digiplan — lihat 2.5 untuk kenapa dan bagaimana. Prinsip arsitektur terkait — lihat [digital-planning.md](../dplan/digital-planning.md) Bagian "Prinsip Arsitektur".)*
- **Excel template upload/download juga perlu mengakomodir 3 kolom ini** — bukan cuma Duration/Man Power/Man Hours (lihat [man-power-man-hours-excel-enhancement.md](../dplan/man-power-man-hours-excel-enhancement.md)). *(Dikonfirmasi 10 Jul 2026 — sebelumnya open question, sekarang jelas: ya, dibutuhkan.)*
- **Validasi saat upload (dikonfirmasi 10 Jul 2026)**: kombinasi nilai Component/Sub Component/Area yang di-upload user divalidasi terhadap Master Data mapping (2.2). Kalau kombinasinya **tidak sesuai/tidak ditemukan** di Master Data, **baris tersebut ditolak** (bukan diproses dengan nilai salah, dan bukan silent-ignore seperti perlakuan Man Hours di [man-power-man-hours-excel-enhancement.md](../dplan/man-power-man-hours-excel-enhancement.md) 3.2) — user diberi **warning yang jelas** (baris mana, kombinasi apa yang tidak valid) supaya bisa memperbaiki data dan upload ulang.
- **Representasi di Excel (dikonfirmasi 10 Jul 2026)**: cell Component/Sub Component/Area di template Excel berupa **kolom text biasa** untuk saat ini — **bukan** Excel data-validation dropdown. Validasi terhadap Master Data baru dilakukan saat upload (lihat poin di atas), bukan dicegah di level cell Excel.

### 2.5 Assess Integrasi ke SAP
- **Tujuan assessment**: memastikan data **Component, Sub Component, Area, Duration, Man Power** ikut **dikirim ke SAP saat create Order** (BAPI call, lihat [order-emol-sap-sync.md](order-emol-sap-sync.md) Bagian 5–6) — bukan cuma disimpan lokal di Digiman+.
- **PIC assessment: Faiza** — Faiza adalah pihak **client**, yang akan berkoordinasi langsung dengan tim SAP internal client-nya untuk assessment ini (bukan tim Digiman+/tim BE). Feasibility teknis di sisi SAP (custom Z-field vs numpang di field text existing) jadi tanggung jawab pembahasan client dengan tim SAP mereka.
- **Kondisi saat ini (gap)**: `Component` dan `Sub Component` **sudah** ikut terbawa sampai ke `PoolingMOItem` (lihat [order-emol-sap-sync.md](order-emol-sap-sync.md) Bagian 5.2), tapi **belum** di-mapping/dikirim ke SAP lewat BAPI saat create Order (lihat mapping `GI_HEADER`/`GI_OPER`/`GI_COMP` di Bagian 6.2 — belum ada field ini). `Area`, `Duration`, `Man Power` juga belum ada sama sekali di `PoolingMOItem`/payload/mapping BAPI.
- **Alasan/manfaat**: kalau data-data ini berhasil dikirim ke SAP saat create Order, maka saat MO tersebut **kembali ke Digiman+ sebagai MO Backlog** (flow inbound, lihat [order-emol-sap-sync.md](order-emol-sap-sync.md) Bagian 9), data Component/Sub Component/Area/Duration/Man Power itu **ikut kembali** bersama MO Backlog-nya. Efeknya: saat user membuat Daily Plan di Digiplan dan memilih MO Backlog, field **Component, Sub Component, Area, Duration, Man Power langsung terisi otomatis** dari data MO Backlog — user tidak perlu input manual lagi di Digiplan.
- **Auto-fill ini tetap bisa di-edit** *(dikonfirmasi 10 Jul 2026)* — nilai Component/Sub Component/Area/Duration/Man Power yang auto-fill dari MO Backlog **bukan read-only**. User yang membuat Plan tetap bisa mengubah nilainya, dengan asumsi user (planner) bisa menganalisa dan menyesuaikan dengan kondisi terkini di lapangan (nilai dari MO Backlog adalah plan awal, bukan keputusan final yang mengikat).
- **Edit di Digiplan murni satu arah** *(dikonfirmasi 10 Jul 2026)* — kalau user mengubah nilai auto-fill ini di Digiplan, perubahan tersebut **tidak sync balik** ke Order/eMOL asal maupun ke SAP. Konsisten dengan prinsip "Digiplan tidak integrasi ke Order" — data mengalir satu arah (Order/eMOL → SAP → MO Backlog → Digiplan), edit di Digiplan berhenti di Digiplan.
- **Beda dengan sync status cancel MO** *(dikonfirmasi 10 Jul 2026)* — perlu dibedakan dari mekanisme di atas: kalau MO di SAP di-**cancel**, **sudah ada** mekanisme existing yang membuat MO Backlog terkait di Digiman+ ikut ter-update/dihapus. Jadi "satu arah" di atas khusus untuk **edit value** (Component/SubComponent/Area/Duration/Man Power) yang tidak sync balik — bukan berarti status/lifecycle MO (cancel) juga tidak ter-sync; status cancel tetap ter-refresh normal lewat mekanisme yang sudah ada.
- *(Ini murni soal memperluas jalur Order/eMOL → SAP yang sudah ada — Digiplan sendiri **tidak** melakukan integrasi/panggilan apapun ke SAP maupun ke Order. Ini juga alasan sebenarnya kenapa Digiplan tidak perlu integrasi langsung dengan Order — lihat [digital-planning.md](../dplan/digital-planning.md) Bagian "Prinsip Arsitektur".)*

### 2.6 Order Approval
- Saat approval Order oleh SPV, SPV **memvalidasi** nilai Man Power dan Duration yang sudah diisi — tapi **tetap bisa mengubah** nilainya di titik approval ini (bukan read-only/lock).
- **Component, Sub Component juga ikut divalidasi & bisa diedit** di titik approval yang sama *(dikonfirmasi 10 Jul 2026, Area dikeluarkan dari daftar ini 16 Jul 2026)* — perlakuan konsisten dengan Man Power/Duration. SPV jadi checkpoint terakhir untuk 4 field ini (Man Power, Duration, Component, Sub Component) sebelum data dikirim ke SAP. **Area tidak divalidasi/diedit terpisah** — ikut ter-derive ulang otomatis di backend kalau SPV mengubah Component/Sub Component (konsisten dengan 2.2/2.3: Area bukan field yang di-expose di layar ini).
- **Aturan validasi & mekanisme input tetap sama persis** *(dikonfirmasi 10 Jul 2026)* — bukan form terpisah yang lebih longgar. SPV tunduk pada aturan yang sama seperti di titik lain: Man Power tetap **wajib, integer, `>0`** (2.3/3.1), dan Component/Sub Component tetap lewat **dropdown Master Data dengan cascading 2 level Component→Sub Component** (2.2/2.3, bukan free text) — tanpa dropdown Area terpisah.

---

## 3. Scope Perubahan & Estimasi Effort

*(SP dikalibrasi ke skala Fibonacci `[1,2,3,5,8]` — konvensi tim BUMA ID, lihat metodologi kalibrasi di [area-of-unit-man-power-effort-summary.md](../area-of-unit-man-power-effort-summary.md). Mandays dihitung dari rasio throughput ~0.98 mandays/SP — basis 5 sprint terakhir, konsisten dengan basis velocity yang dipakai untuk estimasi jumlah sprint.)*

| Komponen | Kompleksitas | SP | Mandays | Catatan |
|---|---|---|---|---|
| Master Data UI baru (service `services-asset`): assign `AreaCode` ke baris `ModelComponentSubComponent` | Sedang | 5 | 4.9 | *(Direvisi 16 Jul 2026 — desain final: 1 kolom `AreaCode` di `ModelComponentSubComponent`, bukan tabel junction M:N)* Master data `Area` baru + UI assign Area per baris existing; SP tidak naik ke 8 karena skema lebih simpel dari draf sebelumnya, tapi regression scope lebih lebar karena tabel yang di-ALTER dipakai fitur lain (Rating, DamageCode) — **masih bergantung validasi asumsi 1:N di Bagian 4, berpotensi berubah kalau ada edge case >1 Area per kombinasi** |
| Permission code baru untuk maintain Master Data (admin HO) | Kecil | 1 | 1.0 | Setup permission code + assign ke role |
| Inspection & Additional Inspection: tambah field Man Power di create finding | Kecil–Sedang | 2 | 2.0 | *(Direvisi 16 Jul 2026 — Area bukan field UI, cukup derive+simpan backend)* Component/Sub Component sudah ada (cascading 2 level tidak berubah), tinggal extend +1 field Man Power + logic backend lookup `AreaCode` — 1 form yang sama berlaku untuk Inspection maupun Additional Inspection, tidak perlu effort terpisah |
| Additional Order: tambah field Component, Sub Component, Duration, Man Power di create screen | Sedang | 2 | 2.0 | *(Direvisi 16 Jul 2026 — Area dikeluarkan dari field UI)* Field baru di layar ini (belum ada sebelumnya, layar terpisah dari Inspection) + cascading dropdown 2 level (Component→Sub Component, sama pola dengan Inspection) + validasi + backend derive Area |
| Edit eMOL: carry-forward nilai dari Finding/Additional Order (auto-fill, tetap editable) | Kecil–Sedang | 2 | 2.0 | Passing value ke layar edit eMOL, bukan re-input |
| Order Approval: tambah validasi & edit Man Power, Duration, Component, Sub Component | Sedang | 2 | 2.0 | *(Direvisi 16 Jul 2026 — Area dikeluarkan dari field yang divalidasi/diedit, ikut re-derive otomatis kalau Component/Sub Component berubah)* UI baru di layar approval + reuse validasi yang sama (dropdown Master Data 2 level, Man Power integer >0) |
| `PoolingMOItem`/payload: tambah kolom Area, Duration, Man Power (Component/Sub Component sudah ada) | Sedang | 3 | 2.9 | Extend insert query (5.2) & payload (6.1) di [order-emol-sap-sync.md](order-emol-sap-sync.md) |
| Mapping BAPI: kirim Component, Sub Component, Area, Duration, Man Power ke SAP | **Belum bisa diestimasi** | — | — | Tergantung hasil assessment client dengan tim SAP mereka (2.5) — bisa kecil (numpang field text) atau besar/multi-sprint (custom Z-field, butuh kerja sisi SAP client di luar kontrol estimasi tim Digiman+) |
| MO Backlog inbound: parse balik Component, Sub Component, Area, Duration, Man Power dari SAP response | Sedang | 3 | 2.9 | Extend parsing/mapping saat pull MO Backlog |
| ~~MO Backlog filter jadi konfigurasi per-client (bukan hardcode)~~ | — | **0** | **0** | **✅ Diputuskan (17 Jul 2026): bukan lagi item dev Digiman+.** Arah berubah — tidak perlu bangun config filter di sisi Digiman+ sama sekali. Tanggung jawab filtering dipindah ke **sisi tenant/middleware**: client yang menentukan MO mana yang dikirim ke Digiman+ (lewat konfigurasi middleware/SAP mereka sendiri), bukan Digiman+ yang filter dari MO yang sudah diterima. Alasan: **fleksibilitas** — tiap client bisa atur sendiri tanpa Digiman+ perlu bangun & maintain config UI/logic filter. Digiman+ tinggal terima & proses apapun yang dikirim middleware. *(Catatan scope: keputusan ini didiskusikan dalam konteks [maintenance-activity-type-enhancement.md](maintenance-activity-type-enhancement.md) tapi effort-nya tercatat di sini karena baris aslinya ada di tabel ini, bukan di dokumen itu.)* |
| Testing end-to-end (Finding/Additional Order → eMOL → Approval → SAP → MO Backlog → Digiplan) | Besar | 8 | 7.8 | Banyak titik integrasi lintas service (`maintenance-execution`, `maintenance-order`, `dplan`), perlu test tiap checkpoint + regresi |
| **Total (di luar mapping BAPI)** | | **28** | **~27** | *(Direvisi 17 Jul 2026, turun dari 31 SP — MO Backlog filter config dihapus dari scope dev Digiman+)* 1 angka pasti (bukan range) — hasil kalibrasi Fibonacci per baris |

*Catatan: estimasi berdasarkan deskripsi arsitektur dari pemilik produk, tanpa akses langsung ke source code — perlu divalidasi oleh engineer yang pegang codebase `maintenance-execution`/`maintenance-order`. Skema mapping Area sudah final secara desain (16 Jul 2026: 1 kolom `AreaCode` di `ModelComponentSubComponent`), tapi masih bergantung validasi asumsi 1:N (lihat Bagian 4) — kalau asumsi itu gugur, Master Data UI perlu didesain ulang jadi M:N/junction table dan SP-nya naik. Mapping BAPI tetap sumber ketidakpastian terbesar lainnya (2.5).*

---

## 4. Open Items / Belum Dibahas

- Assessment SAP integration (2.5) belum ada hasil — masih assignment ke Faiza.
- ~~Apakah mapping Area↔Component-SubComponent ikut scoped per Asset Model atau generik lintas Asset Model~~ — **✅ resolved 16 Jul 2026**: scoped per Model, mapping Area menempel di baris `ModelComponentSubComponent` lewat 1 kolom `AreaCode` baru (bukan tabel mapping 3/4 dimensi terpisah). Lihat 2.2.
- **⚠️ Validasi asumsi 1:N (bukan M:N) untuk relasi Area per baris `ModelComponentSubComponent`** *(open item baru, 16 Jul 2026)* — keputusan desain 1 kolom `AreaCode` mengasumsikan 1 kombinasi Model+Component+SubComponent maksimal punya 1 Area. Asumsi ini logis (1 part fisik = 1 lokasi) tapi **belum divalidasi ke data real/mechanical SME**. Perlu dicek sebelum Master Data UI final — kalau ternyata ada Component yang secara fisik membentang lintas beberapa Area untuk model tertentu, desain perlu direvisi ke M:N/junction table (lihat 2.2, dampak ke estimasi effort di Bagian 3).
- ~~Urutan visual field di form (Inspection/Additional Order/Digiplan/Approval) — apakah Area ditampilkan sebelum Component/Sub Component~~ — **✅ resolved 16 Jul 2026, jadi moot**: Area ternyata **tidak ditampilkan sama sekali** di Inspection/Additional Order/Approval (2.2/2.3/2.6) — cuma di Digiplan sebagai dropdown aktif duluan (2.4). Tidak ada lagi pertanyaan soal urutan visual di 3 layar itu karena field-nya memang tidak ada.
- **Dependency urutan pengembangan**: karena Area di Digiplan (2.4) diisi lewat dropdown dari Master Data mapping (2.2), Master Data UI itu perlu selesai/tersedia lebih dulu sebelum kolom Digiplan ini bisa dikembangkan penuh — urutan rilis/sprint perlu memperhitungkan dependency ini. *(Inspection/Additional Order/Approval tidak punya dependency yang sama karena Area di situ murni backend lookup, bukan dropdown Master Data — bisa dikembangkan independen dari Master Data UI selesai atau belum, asalkan `AreaCode` di `ModelComponentSubComponent` sudah ada kolomnya meski datanya belum lengkap terisi.)*
- Apakah Component/Sub Component/Area di level parent task Digiplan punya rollup logic tersendiri (mengikuti pola Duration/Man Power) atau murni per-leaf tanpa agregasi ke parent? Belum dibahas.
- **Impact ke Digiman Transaction Report — dibahas next phase** *(dicatat 16 Jul 2026)* — Component/SubComponent sudah jadi kolom report existing ([inspection-result.md](../../report/transaction-report/inspection-result.md), [order-result-compliance.md](../../report/transaction-report/order-result-compliance.md)), tapi **Area, Man Power, Duration belum punya tempat di report manapun**. Belum dianalisa/diestimasi apakah dan bagaimana field-field baru ini perlu masuk ke Digiman Transaction Report — sengaja ditunda ke fase berikutnya, bukan bagian dari scope/estimasi Bagian 3 saat ini.

---

## 5. Referensi
- [../dplan/digital-planning.md](../dplan/digital-planning.md)
- [../dplan/man-power-man-hours-excel-enhancement.md](../dplan/man-power-man-hours-excel-enhancement.md)
- [order-emol-sap-sync.md](order-emol-sap-sync.md) — schema `MechanicOrderSummary`/`MechanicOrderList`, flow Order-eMOL → SAP sync, payload & mapping BAPI
- [maintenance-activity-type-enhancement.md](maintenance-activity-type-enhancement.md) — enhancement terpisah di layar yang sama (screen 1 & 2 Additional Order)
